#include <stdio.h>
#include <conio.h>
#include <math.h>
#define PI 3.142

void main()
{
 float radius, area;
 clrscr();

 printf("Enter the radius of a circle");
 scanf ("%f", &radius);

 area = PI * pow (radius,2);

 printf ("Area of a circle = %5.2fn", area);
}

OUTPUT:
